services:
  redis:
    image: redis:7-alpine
    container_name: chinafacil-redis
    restart: always
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    networks:
      - chinafacil-network

  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: chinafacil-app
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - PUPPETEER_SKIP_DOWNLOAD=true
      - PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium-browser
      - NEW_RELIC_LICENSE_KEY=${NEW_RELIC_LICENSE_KEY}
      - NEW_RELIC_APP_NAME=${NEW_RELIC_APP_NAME:-ChinaFacil Backend}
      - NEW_RELIC_LOG_LEVEL=${NEW_RELIC_LOG_LEVEL:-warn}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - chinafacil-network
    depends_on:
      - redis

volumes:
  redis_data:
    driver: local

networks:
  chinafacil-network:
    driver: bridge









